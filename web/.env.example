# OAD Management UI — Environment Variables
# Copy this file to .env.local and fill in the values.

# Base URL of the OAD API server (no trailing slash).
# Leave empty when running with `oad run server` (same origin).
# Override with an absolute URL when using the Vite dev server against a separate API.
VITE_API_BASE_URL=http://localhost:8080
