version: "2"

run:

linters:
  enable:
    - errcheck       # unchecked errors
    - govet          # go vet checks
    - staticcheck    # comprehensive static analysis (replaces golint, gosimple, stylecheck)
    - unused         # unused code
    - ineffassign    # ineffectual assignments
    - gosec          # security issues (OWASP alignment)
    - gocyclo        # cyclomatic complexity guard
    - misspell       # common spelling mistakes
    - unparam        # always-constant function parameters
    - gocritic       # opinionated style and diagnostic checks
    - exhaustive     # exhaustive switch/map on enum types
    - noctx          # HTTP requests without context
    - bodyclose      # unclosed HTTP response bodies
    - sqlclosecheck  # unclosed sql.Rows / sql.Stmt

  settings:
    gocyclo:
      min-complexity: 15

    gosec:
      excludes:
        - G104  # unhandled errors already caught by errcheck

    gocritic:
      enabled-tags:
        - diagnostic
        - style
      disabled-checks:
        - commentedOutCode

    exhaustive:
      default-signifies-exhaustive: true

  exclusions:
    rules:
      # Test files: relax security and unused-param checks
      - path: _test\.go
        linters:
          - gosec
          - unparam

formatters:
  enable:
    - gofumpt        # stricter formatting than gofmt

  settings:
    gofumpt:
      extra-rules: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  exclude-rules:
    - path: '^(web|node_modules)/'
      linters:
        - errcheck
        - govet
        - staticcheck
        - unused
        - ineffassign
        - gosec
        - gocyclo
        - misspell
        - unparam
        - gocritic
        - exhaustive
        - noctx
        - bodyclose
        - sqlclosecheck
