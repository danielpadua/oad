# Compiled binaries and programs
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test artifacts
*.test
*.out
coverage.out
coverage.html

# Go workspace files
go.work
go.work.sum

# Dependency directories (remove the comment below to ignore vendor)
# vendor/

# Build cache (do not version the local build cache)
.cache/

# IDE and editor directories (VS Code, JetBrains, Vim)
.idea/
.vscode/*
!.vscode/settings.json
*.swp
*.swo
*~
*.iml

# Environment files — NEVER commit secrets
.env
.env.*
!.env.example

# OS generated files (macOS / Windows)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
